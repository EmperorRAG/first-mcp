/**
 * Placeholder module for MCP server factory utilities.
 *
 * @remarks
 * Reserved for future server construction helpers.  Currently empty
 * because the `McpServer` is instantiated directly in the
 * transport layer and in `main.ts`.
 *
 * @module
 */
